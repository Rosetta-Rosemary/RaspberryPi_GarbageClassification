set(PROJECT_PISERVER_DIR "${PROJECT_SOURCE_DIR}/PiServer")

find_package(Qt5Widgets CONFIG REQUIRED)
find_package(Qt5Network CONFIG REQUIRED)
find_package(Qt5Core CONFIG REQUIRED)
find_package(PythonLibs REQUIRED)

 include_directories("./")
 include_directories(${PYTHON_INCLUDE_DIRS})
 include_directories("${PROJECT_SOURCE_DIR}/PythonInterface")
 include_directories("${PROJECT_SOURCE_DIR}/Classify")
 include_directories("${PROJECT_SOURCE_DIR}/LogService")
 include_directories("${PROJECT_SOURCE_DIR}/XmlReader")
 include_directories("${PROJECT_SOURCE_DIR}/KeywordAnalsys")
 include_directories("${PROJECT_SOURCE_DIR}/Network")

add_executable(PiServer PiServer.cpp)
 target_link_libraries(PiServer Qt5::Core)
 target_link_libraries(PiServer Qt5::Widgets)
 target_link_libraries(PiServer Qt5::Network)
 target_link_libraries(PiServer PythonInterface_shared)
 target_link_libraries(PiServer Classify_shared)
 target_link_libraries(PiServer LogService_shared)
 target_link_libraries(PiServer XmlReader_shared)
 target_link_libraries(PiServer KeywordAnalsys_shared)
 target_link_libraries(PiServer Network_shared)